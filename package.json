{
  "name": "briefcase",
  "version": "1.0.0",
  "description": "Send take-home assignments to many candidates at once \u2014 one personalised email each, with tracking by hiring domain.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node scripts/check.js",
    "set-password": "node scripts/set-password.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.72.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "nodemailer": "^9.0.5",
    "openai": "^7.4.0"
  }
}
